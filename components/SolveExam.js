/** @format */

import React from 'react';
import { View } from 'react-native';
import SingleExamPaper from './SingleExamPaper';

const SolveExam = () => {
  return (
    <View style={{ backgroundColor: 'white', flex: 1 }}>
      <SingleExamPaper />
    </View>
  );
};

export default SolveExam;
